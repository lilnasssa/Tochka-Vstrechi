aiogram>=3.7,<4
Pillow>=10.0

# QR-коды бот рисует сам (файл qr.py), дополнительных библиотек не надо.
# Ниже - только для СКАНЕРА QR по фото. Без неё бот работает,
# просто код придётся ввести текстом. Если bothost ругается на размер -
# смело удаляйте эту строку.
opencv-python-headless>=4.9
